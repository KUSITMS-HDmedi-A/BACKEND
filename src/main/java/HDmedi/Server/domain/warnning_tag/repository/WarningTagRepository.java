package HDmedi.Server.domain.warnning_tag.repository;

import HDmedi.Server.domain.warnning_tag.entity.WarningTag;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WarningTagRepository extends JpaRepository<WarningTag, Long> {
}
